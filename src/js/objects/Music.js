/* JavaScript*/

class Music{
  constructor(songId, title, artist, img_link){
    this.songId = songId,
    this.title = title,
    this.artist = artist,
    this.img_link = img_link
  }
}

export default Music;